<Project>

  <!--
    One version for the whole product. The release workflow refuses to build unless the git tag,
    this number and app.latestVersion in manifest/markitdown-compat.json all agree.
  -->
  <PropertyGroup>
    <Version>0.7.1</Version>
  </PropertyGroup>

</Project>
