# The fixtures under tests/ must not be touched: a PDF stores absolute byte offsets, so rewriting
# its line endings moves every one of them and the file stops parsing. The Office ones are zips.
* text=auto

*.pdf  binary
*.docx binary
*.xlsx binary
*.pptx binary
*.png  binary
*.ico  binary
*.zip  binary
